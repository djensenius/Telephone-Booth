# Built-in audio tones

`dial-tone.flac` and `beep.flac` are license-free generated assets. They were synthesized from sine waves in this repository (350 Hz + 440 Hz for the dial tone, 1 kHz for the beep) and encoded to FLAC once so normal Cargo builds do not depend on an external `flac` binary.
